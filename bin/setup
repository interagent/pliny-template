#!/usr/bin/env bash

hash foreman 2>/dev/null || {
  echo >&2 "Could not find `foreman` executable. Please install it.";
  exit 1;
}

cp .env.sample .env && bundle install && bundle exec rake db:setup db:schema:load db:migrate
