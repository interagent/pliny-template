module Endpoints
  class <%= plural_class_name %> < Base
    namespace "<%= url_path %>" do
      before do
        content_type :json
      end

      get do
        "[]"
      end

      post do
        "{}"
      end

      delete "/:id" do |id|
        "{}"
      end

      patch "/:id" do |id|
        "{}"
      end
    end
  end
end
