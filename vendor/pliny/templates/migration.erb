Sequel.migration do
  change do
    create_table(:<%= table_name %>) do
      primary_key :id
      datetime    :created_at
      datetime    :updated_at
    end
  end

  down do
    drop_table(:<%= table_name %>)
  end
end
